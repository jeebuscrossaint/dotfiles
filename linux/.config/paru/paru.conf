[options]
BottomUp
SudoLoop
CleanAfter
# paru v2.1.0 corrupts its own completion cache on refresh; keep it from ever
# regenerating. The cache is kept fresh by ~/.local/bin/refresh-paru-completions
# (throttled from config.fish). See that script for details.
CompletionInterval = 3650000
