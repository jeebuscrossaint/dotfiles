#!/bin/sh
# tofi, not rofi: rofi is not installed — tofi is the launcher everywhere else
# (sway's $menu is tofi-drun, theme-pick uses tofi).
cliphist list | tofi --prompt-text "clip ❯ " | cliphist decode | wl-copy
